# Work Queue

Purpose: keep a live, ordered queue of actionable work and execute items continuously.

## Status Keys
- `queued`
- `in_progress`
- `blocked`
- `done`

## Queue
| ID | Status | Priority | Task | Owner | Next Action |
|---|---|---|---|---|---|
| WQ-001 | done | P1 | Finish current CA1861 cleanup slice in tests. | agent | Completed 2026-03-05: `bash scripts/runbook.sh docker-tests` passed (`390/390`), `CA1861` count is `0`, and warning baseline moved to `410`. |
| WQ-002 | done | P1 | Commit CA1861 cleanup once tests are green. | agent | Completed 2026-03-05 in commit `2b60aedb1` (`chore: add work queue and clear CA1861 test warnings`). |
| WQ-003 | done | P2 | Continue zero-risk warning reduction from dominant buckets. | agent | Completed 2026-03-05: `MSTEST0037` backlog cleared in touched suites and Avalonia `AVLN3001` eliminated. Latest `docker-tests` run passed (`390/390`) with build stage `0 Warning(s)`, `0 Error(s)`. |
| WQ-004 | done | P1 | Push `Docker` branch updates to `origin`. | user/background | Completed 2026-03-05 via background handoff; `Docker` and `origin/Docker` are aligned at `efa8efa46`. |
| WQ-005 | done | P2 | Continue `MSTEST0037` reduction batch 2 in top warning files. | agent | Completed 2026-03-05: converted safe numeric/null `Assert.IsTrue` patterns in `ApiIntegrationTests`, `MigrationComplianceTests`, and `CharacterSectionServiceTests`; `docker-tests` passed (`390/390`) and warning baseline moved to `128`. |
| WQ-006 | done | P2 | Continue `MSTEST0037` reduction batch 3 on collection assertions. | agent | Completed 2026-03-05: rewrote high-volume `Assert.AreEqual(0, x.Count)` patterns in compliance/ruleset tests and revalidated. `docker-tests` passed (`390/390`) and warning baseline moved to `122`. |
| WQ-007 | done | P2 | Resolve `CA1860` warnings introduced by `Any()` conversions while keeping `MSTEST0037` gains. | agent | Completed 2026-03-05: replaced `IsFalse(x.Any())` with `Assert.IsEmpty(...)` in touched compliance files and reran `docker-tests`; `CA1860` no longer appears in build warnings. |
| WQ-008 | done | P2 | Continue `MSTEST0037` reduction batch 4 with collection-count modernizations. | agent | Completed 2026-03-05: modernized collection-count assertions and additional boolean/comparison assertions across 30 test files, then revalidated with `bash scripts/runbook.sh docker-tests` (`390/390`). |
| WQ-009 | done | P2 | Resolve remaining `AVLN3001` warning from `Chummer.Avalonia/MainWindow.axaml`. | agent | Completed 2026-03-05: added runtime-loader-compatible parameterless `MainWindow` constructor resolved via app services and revalidated with `bash scripts/runbook.sh docker-tests` (`390/390`, build stage `0 Warning(s)`, `0 Error(s)`). |
| WQ-010 | done | P1 | Milestone 1: make `IChummerClient` a strict contract (remove shell default implementations). | agent | Completed 2026-03-05: removed default shell preference/bootstrap behavior from `IChummerClient`, updated all test stubs to implement explicit semantics, and revalidated via `bash scripts/runbook.sh docker-tests` (`390/390`). |
| WQ-011 | done | P1 | Milestone 2: introduce dedicated shell preferences service boundary. | agent | Completed 2026-03-05: added `IShellPreferencesStore`/`IShellPreferencesService`, wired `ShellPreferencesService` + `SettingsShellPreferencesStore`, added `/api/shell/preferences`, and migrated HTTP/in-process clients + tests. Full docker gate passed (`390/390`). |
| WQ-012 | done | P1 | Milestone 3: extend bootstrap/session snapshot with active workspace session fields. | agent | Completed 2026-03-05: bootstrap contracts now include `ActiveWorkspaceId`, shell preferences persist active workspace selection, API/HTTP/in-process/bootstrap cache all honor active workspace restore semantics, and presenter/session restore now trust explicit snapshot workspace before fallback. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`, build stage `0 Warning(s)`). |
| WQ-013 | done | P1 | Milestone 6: fix bootstrap workspace-cache preference drift bug. | agent | Completed 2026-03-05: `ShellBootstrapDataProvider.GetWorkspacesAsync` now seeds cache from `_client.GetShellPreferencesAsync` (not hardcoded SR5) and includes regression coverage in `ShellBootstrapDataProviderTests`. Revalidated with `bash scripts/runbook.sh docker-tests` (`391/391`, build stage `0 Warning(s)`). |
| WQ-014 | done | P1 | Milestone 4: split generic ruleset hosting infrastructure out of `Chummer.Rulesets.Sr5`. | agent | Completed 2026-03-05: added neutral `Chummer.Rulesets.Hosting` module with `AddRulesetInfrastructure`, converted SR5 extension to `AddSr5Ruleset` (plugin-only), rewired infrastructure/desktop/blazor startup registration paths, and updated docker/compliance guardrails. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`, build stage `0 Warning(s)`). |
| WQ-015 | done | P1 | Milestone 5: make shell surface resolution the only renderer-facing shell model. | agent | Completed 2026-03-05: expanded `ShellSurfaceState` with `LastCommandId`, routed active-tab/notice/error/last-command renderer bindings through shell-surface resolution in both Blazor and Avalonia, and updated component/resolver tests. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`, build stage `0 Warning(s)`). |
| WQ-016 | done | P1 | Milestone 7: internalize `WorkspacePayloadEnvelope` in workspace persistence seam. | agent | Completed 2026-03-05: internal workspace persistence now resolves/stores a first-class `WorkspacePayloadEnvelope` (ruleset/schema/payload-kind/payload), `WorkspaceService` reads/writes through envelope payload resolution, and SR5 `.chum5` external import/download semantics remain unchanged. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`). |
| WQ-017 | done | P1 | Milestone 8: hard-gate download publication so deployed manifests cannot remain empty/unpublished. | agent | Completed 2026-03-05: deployment mode is now explicit (`CHUMMER_PORTAL_DOWNLOADS_DEPLOY_ENABLED=true`) with enforced published-version verification and required live verify target in `publish-download-bundle.sh`; runbook `downloads-sync` can activate deployment gates unattended; workflow env/docs/compliance guardrails updated. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`). |
| WQ-018 | done | P1 | Milestone 9: keep Avalonia composition-only and aligned to resolved shell surface model. | agent | Completed 2026-03-05: moved Avalonia shell-frame interpretation into `MainWindowShellFrameProjector`, reduced `MainWindow.StateRefresh` to apply projected frame state, cached workspace-action lookup for selection dispatch, and updated compliance guardrails for projector-based orchestration. Revalidated with `bash scripts/runbook.sh docker-tests` (`396/396`). |
| WQ-019 | done | P1 | Post-milestone regression rerun for UI/portal e2e flows. | agent | Completed 2026-03-05 with host Docker access: `RUNBOOK_MODE=ui-e2e` and `RUNBOOK_MODE=portal-e2e` both passed, including Playwright execution (`ui E2E completed`, `portal E2E completed`). |
| WQ-020 | done | P1 | Harden UI e2e script for sandbox-unreachable host probes and add guardrail coverage. | agent | Completed 2026-03-05: `scripts/e2e-ui.sh` now supports bounded host-probe + Docker-fallback probing (`CHUMMER_E2E_DOCKER_FALLBACK`, `CHUMMER_E2E_HOST_PROBE_ATTEMPTS`, `CHUMMER_E2E_DOCKER_PROBE_ATTEMPTS`) and compliance checks updated in `MigrationComplianceTests`. Syntax-validated with `bash -n`; full e2e rerun remains blocked in this sandbox by Docker daemon permission + missing host `dotnet`. |
| WQ-021 | done | P2 | Improve runbook e2e summaries for Docker-daemon permission failures. | agent | Completed 2026-03-05: `scripts/runbook.sh` UI/portal e2e summaries now include Docker socket permission-denied signatures so unattended logs clearly identify environment-level blockers. |
| WQ-022 | done | P1 | Add non-CI soft-fail path for Docker-daemon permission errors in e2e scripts. | agent | Completed 2026-03-05: `scripts/e2e-ui.sh` and `scripts/e2e-portal.sh` now soft-skip daemon-permission-denied flows outside CI (`CHUMMER_E2E_PLAYWRIGHT_SOFT_FAIL`, default on locally/off in CI), while CI remains strict; runbook summaries + compliance guardrails updated. |
| WQ-023 | done | P1 | Split shell preferences from shell session state and APIs. | agent | Completed 2026-03-05: introduced `ShellPreferences` + `ShellSessionState` contracts, added `/api/shell/session` + dedicated session store/service, updated HTTP/in-process clients and shell presenter/bootstrap data provider to persist preferred ruleset and active workspace independently, and added integration/compliance coverage. |
| WQ-024 | done | P1 | Route workspace behavior through explicit ruleset codec seam. | agent | Completed 2026-03-05: added `IRulesetWorkspaceCodec` + resolver seam, introduced `Sr5WorkspaceCodec`, wired DI registration, and refactored `WorkspaceService` to route import/summary/section/validation/metadata flows through codec dispatch while preserving envelope-backed SR5 `.chum5` behavior. |
| WQ-025 | done | P1 | Extend shell session snapshot to persist active tab context. | agent | Completed 2026-03-05: `ShellSessionState` + bootstrap contracts now carry `ActiveTabId`, session endpoint/store persist it, clients/bootstrap provider flow it through, and shell presenter restores/persists tab context explicitly. |
| WQ-026 | done | P2 | Persist active tab per workspace session map. | agent | Completed 2026-03-05: shell session/bootstrap contracts now carry `ActiveTabsByWorkspace`, API/runtime/http/presenter/bootstrap cache propagate and normalize the map, and regression coverage was added across integration/presenter/compliance suites. |
| WQ-027 | done | P1 | Expand desktop downloads matrix to include macOS Intel (`osx-x64`) for Avalonia and Blazor Desktop. | agent | Completed 2026-03-05: added `osx-x64` matrix rows for both desktop heads, added macOS x64 RID labeling in portal/manifest generation, and validated via preapproved `bash scripts/runbook.sh desktop-gate` + `RUNBOOK_MODE=downloads-manifest`. |
| WQ-028 | done | P2 | Move shell bootstrap composition helper off `IChummerClient` default interface implementation. | agent | Completed 2026-03-05: confirmed `IChummerClient` remains signature-only and added compliance/runbook guardrails that forbid default `GetShellBootstrapAsync` method bodies on the interface contract. |
| WQ-029 | done | P2 | Harden preapproved runbook `local-tests` mode for sandbox/no-escalation execution. | agent | Completed 2026-03-05: `local-tests` now supports single-proc/build-server-off defaults with environment overrides and optional `--no-restore`/`--no-build`; validated via preapproved `bash scripts/runbook.sh` executions. |
| WQ-030 | done | P3 | Add `local-tests` fast-fail preflight for blocked NuGet connectivity. | agent | Completed 2026-03-05: `local-tests` now probes `api.nuget.org:443` before restore-heavy runs and fails fast with guidance instead of long retry loops in restricted environments. |
| WQ-031 | done | P2 | Add optional object-storage publish path for desktop download bundles. | agent | Completed 2026-03-05: added `scripts/publish-download-bundle-s3.sh`, wired opt-in `deploy-downloads-object-storage` workflow job + env/secret variables, added `runbook` mode `downloads-sync-s3`, and documented S3/R2-compatible deployment flow in README/compliance guardrails. |
| WQ-032 | done | P3 | Strengthen macOS matrix guardrail to require `osx-x64` rows for both desktop heads. | agent | Completed 2026-03-05: compliance assertions now require explicit `osx-x64` matrix blocks for both Avalonia and Blazor Desktop rows instead of generic RID string presence. |
| WQ-033 | done | P1 | Execute targeted/full `local-tests` through preapproved runbook path after dependency restore. | agent | Completed 2026-03-05 via strict preapproved wrapper (`bash scripts/runbook-strict-host-gates.sh`): NuGet preflight passed and strict local tests completed (`349/349`) before docker strict gate (`408/408`). |
| WQ-034 | done | P1 | Enforce self-hosted portal downloads default configuration. | agent | Completed 2026-03-05: `Chummer.Portal/appsettings.json` now defaults `DownloadsBaseUrl` to `/downloads/` and runbook/compliance guardrails enforce no regression to GitHub fallback defaults. |
| WQ-035 | done | P2 | Harden `runbook docker-tests` for daemon-permission blockers. | agent | Completed 2026-03-05: added Docker daemon preflight in `docker-tests` mode with non-CI soft-skip default (`DOCKER_TESTS_SOFT_FAIL`) and clearer permission-denied summaries. |
| WQ-036 | done | P2 | Harden `runbook local-tests` preflight for unattended restricted environments. | agent | Completed 2026-03-05: added non-CI soft-skip default (`TEST_NUGET_SOFT_FAIL`) for NuGet preflight failures so restore-blocked environments skip quickly and continue unattended. |
| WQ-037 | done | P1 | Add operator runbook for self-hosted downloads + strict test unblock steps. | agent | Completed 2026-03-05: added `docs/SELF_HOSTED_DOWNLOADS_RUNBOOK.md` with filesystem + object-storage publish flows and strict local/docker gate commands; wired README and runbook guardrails to keep the operator path discoverable. |
| WQ-038 | done | P3 | Revalidate UI/portal e2e soft-skip behavior in restricted sandbox. | agent | Completed 2026-03-05: `RUNBOOK_MODE=ui-e2e` and `RUNBOOK_MODE=portal-e2e` both exit cleanly with explicit Docker permission skip reasons, preserving unattended queue progression. |
| WQ-039 | done | P1 | Add optional per-artifact link/file verification to downloads manifest validation. | agent | Completed 2026-03-05: `verify-releases-manifest.sh` now supports opt-in per-artifact checks (`CHUMMER_PORTAL_DOWNLOADS_VERIFY_LINKS`), wired through workflow/runbook/deploy scripts and validated with local synthetic bundle checks. |
| WQ-040 | done | P2 | Add strict host gate wrapper script for unattended manual execution. | agent | Completed 2026-03-05: added `scripts/runbook-strict-host-gates.sh` to run strict local+docker gates (`TEST_NUGET_SOFT_FAIL=0`, `DOCKER_TESTS_SOFT_FAIL=0`) in one command and wired docs/guardrails. |
| WQ-041 | done | P2 | Land verification hardening + strict-gate wrapper batch commit. | agent | Completed 2026-03-05 in commit `564953e39`: artifact-link verification hardening, workflow/runbook/docs wiring, and strict host gate wrapper script are merged. |
| WQ-042 | done | P1 | Execute strict host-gate wrapper in this sandbox. | agent | Completed 2026-03-05 with elevated host access: strict prerequisites passed and strict wrapper now runs end-to-end via preapproved script path. |
| WQ-043 | done | P2 | Add host prerequisite check script for strict gate readiness. | agent | Completed 2026-03-05: added `scripts/check-host-gate-prereqs.sh` plus `RUNBOOK_MODE=host-prereqs` with summary output, and wired docs/guardrails/compliance checks. |
| WQ-044 | done | P2 | Commit prerequisite-check and verification-hardening follow-up batch. | agent | Completed 2026-03-05 in commit `8205d6a81`: host prerequisite runbook mode, verifier hardening wiring, and updated docs/compliance coverage are committed. |
| WQ-045 | done | P1 | Fix host-prereqs log path/summary reliability for non-sudo users. | agent | Completed 2026-03-05: replaced fixed `/tmp` prereq logs with per-user writable-path resolution in `runbook` + `check-host-gate-prereqs`, eliminating permission-denied logging and stale summary mismatches. |
| WQ-046 | done | P2 | Run strict host-gate wrapper through explicit prerequisite phase first. | agent | Completed 2026-03-05: `scripts/runbook-strict-host-gates.sh` now executes `check-host-gate-prereqs.sh` before strict local/docker tests, with runbook/compliance guardrails updated. |
| WQ-047 | done | P1 | Execute strict host gates on operator host and collect results. | agent | Completed 2026-03-05: strict host run now passes local + docker stages (`408/408`) after constructor/session/rules-tab regressions were fixed. |
| WQ-048 | done | P2 | Add strict-host worktree drift guard for unattended runs. | agent | Completed 2026-03-05: `runbook-strict-host-gates.sh` now snapshots tracked `git` status before/after and fails on unexpected drift unless `STRICT_ALLOW_WORKTREE_DRIFT=1` is explicitly set; docs updated. |
| WQ-049 | done | P2 | Clear remaining strict-gate `MSTEST0037` and `CA1859` warnings in shell/session presenter test stubs. | agent | Completed 2026-03-05: replaced `Assert.IsTrue(count > 0)` with `Assert.IsNotEmpty` in `ShellPresenterTests` and updated `NormalizeWorkspaceTabMap` helper return types to `Dictionary<string, string>?` in affected test stubs; strict host wrapper revalidated (`349/349` local, `408/408` docker). |
| WQ-050 | done | P1 | Implement explicit portal downloads fallback setting separate from self-hosted downloads base URL. | agent | Completed 2026-03-05: added `DownloadsFallbackUrl`/`CHUMMER_PORTAL_DOWNLOADS_FALLBACK_URL`, switched manifest fallback resolution to the new setting, changed missing-file handling to `404` when no fallback is configured, and updated compliance guardrails. Revalidated with `bash scripts/runbook.sh local-tests net10.0 FullyQualifiedName~Chummer.Tests.Compliance.MigrationComplianceTests` and `bash scripts/runbook-strict-host-gates.sh` (`349/349`, `408/408`). |
| WQ-051 | done | P2 | Suppress known legacy-package `NU1701` noise for strict migration test legs. | agent | Completed 2026-03-05: added `NU1701` to test/runtime project warning suppressions for migration targets (`Chummer.Tests/Chummer.Tests.csproj`, `Chummer/Chummer.csproj`), yielding cleaner strict-gate restore/build output while preserving test coverage (`349/349`, `408/408`). |
| WQ-052 | done | P2 | Harden portal E2E landing contract checks for required launcher links. | agent | Completed 2026-03-05: `scripts/e2e-portal.cjs` now enforces `/blazor/`, `/avalonia/`, `/downloads/`, `/docs/`, and `/api/health` link presence on `/`; compliance guardrail test updated and revalidated via `bash scripts/runbook.sh local-tests net10.0 FullyQualifiedName~Chummer.Tests.Compliance.MigrationComplianceTests` (`43/43`). |
| WQ-053 | done | P2 | Add CI guardrails for compose validity and portal formatting drift checks. | agent | Completed 2026-03-05: `docker-architecture-guardrails.yml` now includes `compose-config-validation` (`docker compose config`) and `portal-format-guardrail` (`dotnet format ... --verify-no-changes --include Chummer.Portal/Program.cs`), with compliance assertions added and revalidated (`44/44`). |
| WQ-054 | done | P2 | Add unattended parity checklist generation from legacy shell contract coverage. | agent | Completed 2026-03-05: added `scripts/generate-parity-checklist.sh`, `RUNBOOK_MODE=parity-checklist`, generated `docs/PARITY_CHECKLIST.md`, and wired README/compliance guardrails so checklist generation stays discoverable and enforced. Revalidated with targeted compliance runbook tests (`44/44`). |
| WQ-055 | done | P2 | Enforce parity checklist freshness in CI guardrails workflow. | agent | Completed 2026-03-05: added `parity-checklist-sync` job to `.github/workflows/docker-architecture-guardrails.yml` to regenerate via runbook and fail on `docs/PARITY_CHECKLIST.md` drift, with compliance assertions revalidated (`44/44`). |

## Intake Template
Append new items at bottom:

`| WQ-XXX | queued | P1/P2/P3 | <task> | agent/user | <next action> |`
